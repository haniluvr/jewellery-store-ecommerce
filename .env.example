APP_NAME="Éclore"
APP_ENV=local
APP_DEBUG=true
APP_KEY=
# For local development with HTTPS and Google OAuth, use localhost
# APP_URL=https://localhost:8080
# For local development with HTTP, use .test domain
APP_URL=http://eclore.test:8080

LOG_CHANNEL=stack
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

# Database Configuration (SQLite - Default)
# DB_CONNECTION=sqlite
# The SQLite database file is located at: database/database.sqlite
# If you want to use MySQL, uncomment below and configure accordingly
 DB_CONNECTION=mysql
 DB_HOST=127.0.0.1
 DB_PORT=3306
 DB_DATABASE=davids_wood
 DB_USERNAME=root
 DB_PASSWORD=

BROADCAST_DRIVER=log
CACHE_DRIVER=file
FILESYSTEM_DISK=local
QUEUE_CONNECTION=sync
SESSION_DRIVER=database
SESSION_LIFETIME=120

MEMCACHED_HOST=127.0.0.1

REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=log
MAIL_HOST=mailpit
MAIL_PORT=1025
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="noreply@eclore.test"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

PUSHER_APP_ID=
PUSHER_APP_KEY=
PUSHER_APP_SECRET=
PUSHER_HOST=
PUSHER_PORT=443
PUSHER_SCHEME=https
PUSHER_APP_CLUSTER=mt1

VITE_APP_NAME="${APP_NAME}"
VITE_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
VITE_PUSHER_HOST="${PUSHER_HOST}"
VITE_PUSHER_PORT="${PUSHER_PORT}"
VITE_PUSHER_SCHEME="${PUSHER_SCHEME}"
VITE_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"

# Force HTTPS (set to true if using SSL)
FORCE_HTTPS=false

# Google OAuth Configuration
# NOTE: Google OAuth does NOT allow .test domains
# For local development with HTTPS, use localhost or 127.0.0.1
# Get credentials from: https://console.cloud.google.com/
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# For local HTTPS development, use: https://localhost:8080/auth/google/callback
# For production, use: https://yourdomain.com/auth/google/callback
GOOGLE_REDIRECT_URL=

# IMPORTANT: When using localhost for OAuth:
# - Main site: https://localhost:8080
# - Admin area: https://admin.localhost:8080
# 
# When using .test domain (without OAuth):
# - Main site: http://eclore.test:8080  
# - Admin area: http://admin.eclore.test:8080


